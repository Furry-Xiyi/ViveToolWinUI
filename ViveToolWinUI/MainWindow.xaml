<Window
    x:Class="ViveToolWinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:muxc="using:Microsoft.UI.Xaml.Controls"
    Title="ViveTool WinUI">

    <Grid x:Name="RootLayout" MinWidth="500" MinHeight="300">
        <Grid.RowDefinitions>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 自定义标题栏 -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Background="Transparent" Padding="12,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Image Source="Assets/Square44x44Logo.png" Width="20" Height="20" Margin="0,0,10,0"/>
                <TextBlock Text="ViveTool WinUI" FontSize="16" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- NavigationView 主体 -->
        <muxc:NavigationView x:Name="NavView"
                             Grid.Row="1"
                             IsSettingsVisible="True"
                             PaneDisplayMode="Auto"
                             IsBackButtonVisible="Visible"
                             IsBackEnabled="False"
                             SelectionChanged="NavView_SelectionChanged"
                             BackRequested="NavView_BackRequested">
            <muxc:NavigationView.MenuItems>

                <!-- 快速操作 -->
                <muxc:NavigationViewItem x:Uid="Nav_QuickActions" Tag="QuickActions">
                    <muxc:NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8BD;" FontFamily="Segoe Fluent Icons"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <!-- 查找可用 ID -->
                <muxc:NavigationViewItem x:Uid="Nav_IDFinder" Tag="IDFinder">
                    <muxc:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Find"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

                <muxc:NavigationViewItemSeparator/>

                <!-- 输出日志 -->
                <muxc:NavigationViewItem x:Uid="Nav_Output" Tag="Output">
                    <muxc:NavigationViewItem.Icon>
                        <SymbolIcon Symbol="Document"/>
                    </muxc:NavigationViewItem.Icon>
                </muxc:NavigationViewItem>

            </muxc:NavigationView.MenuItems>

            <Frame x:Name="ContentFrame"
                   Navigated="ContentFrame_Navigated"/>
        </muxc:NavigationView>

        <!-- 顶部通知栏 -->
        <Grid x:Name="InfoBarOverlay"
              Grid.RowSpan="2"
              VerticalAlignment="Top"
              IsHitTestVisible="False"
              Margin="0,8,0,0">
            <muxc:InfoBar x:Name="TopInfoBar"
                          IsOpen="False"
                          IsClosable="True"
                          Severity="Informational"
                          HorizontalAlignment="Center"
                          MaxWidth="980">
                <muxc:InfoBar.RenderTransform>
                    <TranslateTransform Y="-80"/>
                </muxc:InfoBar.RenderTransform>
            </muxc:InfoBar>
        </Grid>
        <!-- 假 Splash 覆盖层（UWP风格；使用包资源自动缩放的 SplashScreen.png） -->
        <Grid x:Name="SplashOverlay"
Grid.RowSpan="2"
Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
Visibility="Collapsed"
Opacity="0"
IsHitTestVisible="False">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="12">
                <!-- 这里只定义 Image，不写 Source -->
                <Image x:Name="SplashImage"
         Width="220" Height="220"
         Stretch="UniformToFill"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>