<Page
    x:Class="ViveToolWinUI.Pages.OutputPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="16"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="2" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8" Padding="16">
            <ScrollViewer>
                <TextBlock x:Name="TxtOutput" 
                          TextWrapping="Wrap" 
                          IsTextSelectionEnabled="True"
                          FontFamily="Consolas"
                          FontSize="12"/>
            </ScrollViewer>
        </Border>

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="12" HorizontalAlignment="Right">
            <Button x:Name="BtnCopy" x:Uid="Output_Copy" Click="Copy_Click" MinWidth="100"/>
            <Button x:Name="BtnSave" x:Uid="Output_Save" Click="Save_Click" MinWidth="100"/>
            <Button x:Name="BtnBack" x:Uid="Output_Back" Click="Back_Click" MinWidth="100"/>
        </StackPanel>
    </Grid>
</Page>